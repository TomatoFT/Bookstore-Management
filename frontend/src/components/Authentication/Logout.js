import React from 'react';
import './Logout.css'
const Logout = () => {
  const handleLogout = () => {
    // Logic for logging out the user
    // You can clear user session, reset state, etc.
  };

  return (
    <div className='logout'>
      <h2>You are log out from the systems</h2>
    </div>
    );
};

export default Logout;
